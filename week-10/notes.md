# Week 10: Digital Forensics & Incident Response
**Commands Used:**
- `fls -r` & `icat`: I ran these Sleuth Kit tools against a raw disk image to locate the inode of a deleted file and attempt to extract its payload. 
- `curl -X POST`: Used to manually inject raw JSON log data into our local Elasticsearch database.
**TLAB-10 (Operation Phantom Pursuit):** Completed. Reconstructed an adversary's kill chain using ELK stack correlation and conducted a deep digital autopsy on a compromised container.
